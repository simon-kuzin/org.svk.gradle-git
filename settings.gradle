 pluginManagement {
    repositories {
        maven {
            url uri("file:./build/repo/")
        }
        gradlePluginPortal()
        jcenter()
    }
    
    plugins{
        id "com.gradle.plugin-publish" version "0.10.1"
    }
}

include 'gradle-git'
